Para conectar mysql con Django debemos copiar los siguientes comandos:

pip install cx_Oracle

pip install django

pip install djangorestframework
pip install django-cors-headers

sudo docker exec -it oracle-xe-container bash

sqlplus sys/root@localhost:1521/XE as sysdba

CREATE PLUGGABLE DATABASE gestion_inventario ADMIN USER vides IDENTIFIED BY root FILE_NAME_CONVERT = ('/opt/oracle/oradata/XE/pdbseed/', '/opt/oracle/oradata/XE/gestion_inventario/');

ALTER PLUGGABLE DATABASE gestion_inventario OPEN;

--------------------------------------------------------

__ubicarse en la capeta usuario

mkdir -p ~/oracle-instantclient
unzip instantclient-basic-linux.x64-VERSION.zip -d ~/oracle-instantclient
unzip instantclient-sdk-linux.x64-VERSION.zip -d ~/oracle-instantclient

export LD_LIBRARY_PATH=~/oracle-instantclient/instantclient_19_8:$LD_LIBRARY_PATH

source ~/.bashrc

__ejecutar la virtual_3

pip install cx_Oracle --upgrade

 ls /home/isaza/oracle-instantclient/instantclient_23_5

export LD_LIBRARY_PATH=/home/isaza/oracle-instantclient/instantclient_23_5:$LD_LIBRARY_PATH

echo 'export LD_LIBRARY_PATH=/home/isaza/oracle-instantclient/instantclient_23_5:$LD_LIBRARY_PATH' >> ~/.bashrc
source ~/.bashrc

sudo add-apt-repository universe
sudo add-apt-repository multiverse
sudo apt-get update

sudo apt-get install libaio1

apt-cache search libaio

wget http://deb.debian.org/debian/pool/main/liba/libaio/libaio1_0.3.112-3_amd64.deb

sudo dpkg -i libaio1_0.3.112-3_amd64.deb

Ingresar al contenedor con bash 

--------------------------------------------------------------------------------------------------
PRIVILEGIOS PARA HACER MIGRACION

lsnrctl start

sqlplus sys/root@localhost:1521/almacen2022_oracle as sysdba

GRANT CONNECT, RESOURCE TO isaza;
GRANT CREATE TABLE, CREATE VIEW, CREATE TRIGGER, CREATE SEQUENCE TO isaza;

CONNECT sys as sysdba;

-- Conéctate a la base de datos pluggable
ALTER SESSION SET CONTAINER=ALMACEN2022_ORACLE;

SELECT username FROM all_users WHERE username = 'ISAZA';

-- Conceder acceso de sesión
GRANT CREATE SESSION TO isaza;

-- Conceder permisos adicionales
GRANT UNLIMITED TABLESPACE TO isaza; -- O selecciona un tablespace específico si lo prefieres
GRANT CREATE TABLE TO isaza;

GRANT CREATE SESSION TO ISAZA;
GRANT DROP ANY TABLE TO ISAZA;
GRANT ALTER ANY TABLE TO ISAZA;
GRANT CREATE ANY INDEX TO ISAZA;
GRANT DROP ANY INDEX TO ISAZA;

DROP TABLE "DJANGO_CONTENT_TYPE" CASCADE CONSTRAINTS;
DROP TABLE "DJANGO_MIGRATIONS" CASCADE CONSTRAINTS;
DROP INDEX "DJANGO_CO_APP_LABEL_76BD3D3B_U";

python manage.py migrate

python3 manage.py createsuperuser
